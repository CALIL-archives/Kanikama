machine:
  node:
    version:
      0.12.7
dependencies:
  post:
    - npm update
    - ./node_modules/.bin/gulp compile_test
test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/mocha && node ./node_modules/.bin/mocha -R xunit > $CIRCLE_TEST_REPORTS/mocha/testResults.xml
